/*Enum is a special class that holds one/multiple unchangable values (like final attributes) with which we can acees 
or give it to some attribute that should not accept any value but the ones declared in the enum class */

//NOTE: the enum values should be in capital upercases

//NOTE: you can have an enum inside a class (like you have a table, where you declare you constants)

enum Enum {
ENUMONE,
ENUMTWO,
ENUMTHREE
}
