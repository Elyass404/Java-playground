import java.util.TreeSet; 
import java.util.Set; 

//The treeset is like the hashsetbut the difference happens in the sorting of the elements inside the treeset
//the treeset automatically sorts the elements even if we entered them not sorted

public class Treeseto{

    public static void main(String[] args){

        Set<String> theTree = new TreeSet<>();

        theTree.add("elementOne");


        System.out.println(theTree);
    
    }

    
}